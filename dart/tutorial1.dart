void main(){
  int age = 30; //defining a integer in dart
  String name = 'Abhay Gupta';//defining a string in dart
  bool isSmart = true; //defining the bool type 
  dynamic vari = 'Abhay'; // this is dynamic variable, which can be changed in future means it contain all data types - int, string , boolean, float etc 
  vari = 30; //above it is string here it is int
  print(age);
  print(name);
  print(isSmart);
  print(vari);
}