from numpy import *

n1 = array([10, 20, 30, 40, 50])
#this will find the mean of the elemets of the numpy array
print(mean(n1))

#this will find the standard deviation of the numpy array
print(std(n1))

#this will find the median of the numpy array
print(median(n1))